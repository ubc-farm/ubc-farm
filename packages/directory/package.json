{
  "name": "@ubc-farm/directory",
  "version": "2.1.0",
  "dependencies": {
    "@ubc-farm/utils": "^4.2.0",
    "@ubc-farm/databases": "^2.0.0",
    "document-promises": "^3.1.2",
    "react-virtualized": "9.0.1"
  },
  "peerDependencies": {
		"react": "^15.4.2",
		"react-dom": "^15.4.2"
	},
  "devDependencies": {
		"rollup": "^0.41.4"
	},
  "scripts": {
    "build-index": "rollup -c -i entry/index.ts -o browser/index.js",
    "watch": "rollup -c -w -i entry/index.ts -o browser/index.js",
    "prepare": "npm run build-index --quiet",
    "prepublish": "npm run prepare --quiet"
  }
}
