{
	"name": "@ubc-farm/react-inputs",
	"version": "1.0.0",
	"module": "index.mjs",
	"main": "index.js",
	"dependencies": {
		"moment": "^2.17.1",
		"react-virtualized-select": "^2.3.0",
		"@ubc-farm/utils": "^4.1.0"
	},
	"devDependencies": {
		"rollup": "^0.41.4",
		"typescript": "^2.2.1",
		"shortid": "^2.2.8"
	},
	"optionalDependencies": {
		"react": "^15.4.2"
	},
	"scripts": {
		"ts": "tsc --noEmit || exit 0",
		"build:es": "rollup -c -f es -i src/index.ts -o index.mjs",
		"build:cjs": "rollup -c -f cjs -i src/index.ts -o index.js",
		"prepare": "npm run build:es && npm run build:cjs",
		"prepublish": "npm run prepare"
	}
}
